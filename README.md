# MultiAgentFamilyTaskOrganizer
An on-device AI that runs several parallel conversations from a single small local model, each scoped to a different person in the household. Same request, different outcome depending on who's asking. The model only proposes an action; a deterministic permission layer decides yes / no / ask-a-human; every decision is logged. 
